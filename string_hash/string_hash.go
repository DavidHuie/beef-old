package string_hash

import "hash/fnv"

// Hash takes in a string to hash and an integer
// that corresponds to which hash function to apply.
func Hash(str string, hash_number int) uint64 {
	return uint64(0)
}
